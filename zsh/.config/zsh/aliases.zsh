#!/bin/sh

# nvim
alias vi="nvim"

# confirm before overwriting something
alias cp="cp -i"
alias mv="mv -i"
alias rm="rm -i"

# eza
alias ls='eza --group-directories-first --icons=auto'
alias ll="ls -lh --git"
alias la='ll -a'
alias lt='ls -a --tree'
